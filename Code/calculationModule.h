#include <math.h>
/*
	Contains functions for generic calculations, which cannot be assigned to any 
	specific class.
*/


float calculateTurningRadius(float, float);
float decreaseBy(float, float);
bool inRange(float, float, float);
float bringInRange(float, float, float);
